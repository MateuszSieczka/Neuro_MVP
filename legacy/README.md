# legacy/ — reference only (NOT importable)

Spiking-era code preserved as a **specification** for refactoring onto the
Phase-U predictive-coding substrate. These files import deleted modules on
purpose — do not run or import them. Read them, reimplement on the
substrate.

See **`../LEGACY_INTEGRATION.md`** for the per-file decision (restore+fix /
rewrite), how each maps onto the substrate, and the integration order.

Discarded (not here; in git history at `8009b22`): `core/m1.py`,
`core/learning_pipeline.py`, `core/receptor.py`.
